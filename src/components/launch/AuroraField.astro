<canvas id="gl" data-od-id="aurora-field" aria-hidden="true"></canvas>
<div class="fallback" data-od-id="webgl-fallback"></div>

<noscript>
  <style>
    .fallback {
      display: block;
    }
    #gl {
      display: none;
    }
  </style>
</noscript>

<script>
  import { initAurora } from '../../scripts/aurora';

  const canvas = document.getElementById('gl');
  if (canvas instanceof HTMLCanvasElement) {
    initAurora(canvas);
  }
</script>
